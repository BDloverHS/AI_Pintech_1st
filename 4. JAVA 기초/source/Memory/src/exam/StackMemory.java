package exam;

public class StackMemory {
    /**
     * 스택 영역 메모리
     * 함수가 연산을 수행할 때만 공간을 할당. 작업 완료(return) 후 제거
     * 제거되는 이유 : 컴퓨터 자원은 유한하다. 안 쓰는 건 제거함
     *
     * 스택 구조 : 가장 먼저 넣은 자료는 마지막에. 가장 마지막에 넣은 자료를 맨 처음에 꺼내는 구조
     * 큐 구조 : 먼저 넣은 순서대로 먼저 나감
     *
     * 스택 영역 메모리(호출 스택)
     */
}
